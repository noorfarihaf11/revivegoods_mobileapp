import 'package:flutter/material.dart';

// Simple color constants for the app
// This is a basic way to maintain consistent colors across the app
class AppColors {
  // Main colors
  static const Color primary = Color(0xFF4B367C);
  static const Color secondary = Color(0xFFB5A9F8);
  static const Color background = Colors.white;

  // Text colors
  static const Color textDark = Color(0xFF4B367C);
  static const Color textGrey = Color(0xFF6E6E6E);

  // Other UI colors
  static const Color divider = Color(0xFFE0E0E0);
  static const Color coinColor = Color(0xFFFFA726);
}
